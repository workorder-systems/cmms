export const metadata = {
  title: 'Installation',
  description: 'Install and use @workorder-systems/db in browser, Node, and edge.',
}

export const sections = [
  { title: 'Install the package', id: 'package' },
  { title: 'Peer dependency', id: 'peer-dependency' },
  { title: 'Create a client', id: 'usage' },
  { title: 'Edge runtimes', id: 'edge-runtimes' },
  { title: 'Environment variables', id: 'environment-variables' },
]

# Installation

Add **`@workorder-systems/db`** to get a typed client over the project’s public views and RPCs. The package ships compiled **`dist/`** output and declarations, with no compile step in your app. {{ className: 'lead' }}

## Install the package

<CodeGroup title="Install">

```bash
npm install @workorder-systems/db @supabase/supabase-js
```

</CodeGroup>

<Note>
  In this monorepo the workspace package is named **`@workorder-systems/sdk`**. Published npm name is **`@workorder-systems/db`**.
</Note>

## Peer dependency

Install **`@supabase/supabase-js`** alongside the SDK (peer range **`^2.39.0`**). The SDK builds on the Supabase client; your app supplies both packages.

## Create a client

Pass the project URL and anon key from **Project Settings → API**:

```ts
import { createDbClient } from '@workorder-systems/db'

const client = createDbClient(
  process.env.SUPABASE_URL!,
  process.env.SUPABASE_ANON_KEY!
)

// client.supabase: typed Supabase client
// client.setTenant(tenantId) / client.clearTenant()
// Domain resources:
//   client.tenants, client.workOrders, client.assets, client.locations, client.departments, client.meters
//   client.plugins, client.authorization, client.catalogs, client.pm, client.dashboard, client.audit
```

Same pattern works in the browser and in Node. For **edge** runtimes (Cloudflare Workers, Vercel Edge, etc.), pass the platform **`fetch`** and turn off session persistence if you do not need it:

```ts
const client = createDbClient(url, anonKey, {
  global: { fetch },
  auth: { persistSession: false },
})
```

<div className="not-prose flex flex-wrap gap-3">
  <Button href="/quickstart" variant="text" arrow="right">
    <>Quickstart</>
  </Button>
</div>

## Edge runtimes

Use **`global: { fetch }`** so requests use the host runtime. Use **`auth: { persistSession: false }`** when there is no durable session store. For custom storage, set **`auth.storage`** (Supabase-compatible session storage).

## Environment variables

The SDK expects **`SUPABASE_URL`** and **`SUPABASE_ANON_KEY`** in the environment you run it in. Deployed Edge Functions get all **`SUPABASE_*`** values from Supabase automatically. See [Configuration](/configuration).

<div className="not-prose flex flex-wrap gap-3">
  <Button href="/configuration" variant="text" arrow="right">
    <>Configuration</>
  </Button>
</div>
