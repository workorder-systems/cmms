# @db/sdk Documentation

Welcome to the docs for the Work Order Systems database SDK (`@db/sdk`).

This site is built with **Nextra 4** (Docs Theme) and will eventually
include:

- Getting started guides for using `createDbClient()`
- Auth and tenant context walkthroughs
- Resource guides for tenants, work orders, assets, locations, and more
- Live examples that talk to a demo Supabase project

For now, see the main SDK README in the repo:

- [`packages/sdk/README.md`](../packages/sdk/README.md)

