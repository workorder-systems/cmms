// Optional MDX components mapping. You can extend this later with
// custom components that you want to use across your docs.
export function useMDXComponents(components) {
  return {
    ...components,
  };
}

